import MicroModal from 'micromodal'

export default function modals() {
  // Init the modals - this might need to happen after they are created
  MicroModal.init()

  const gem1 = document.querySelector('#gem1')
}
